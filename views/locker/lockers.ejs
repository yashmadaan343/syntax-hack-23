<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Deposit</title>
</head>
<style>
    @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@500&display=swap');
    body {
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
        font-family: 'Outfit';
        background: 50%/contain url("https://www.halo-lab.com/cdn-cgi/image/anim=true,dpr=1,format=auto,quality=85/cloudflare-images/universe.f742a005ecd.png?66d5e0a3733") #02021e;
        font-size: max(16px,.9rem);
        font-weight: 300;
        margin: 0;
        min-height: 100vh;
        overflow-x: hidden;
        padding: 0;
        position: relative;
        color: #fff;
        animation: fadeIn 1s 1;
    }

    @keyframes fadeIn {
        0% {
            opacity: 0;
            transform: translateY(10px);
        }
        100% {
            opacity: 1;
            transform: translateY(auto);
        }
    }

    nav {
        height: 7.5vw;
        color: #fff;
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        align-items: center;
        border-bottom: 1px solid #fff;
    }

    .links {
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        gap: 3rem;
        margin-right: 2vw;
        list-style: none;
    }

    .loginUl {
        list-style-type: none;
        margin-right: 5vw;
    }

    .logo {
        margin-left: 5vw;
        font-size: 2rem;
    }

    .link {
        text-decoration: none;
        color: #ffda93;
        font-size: 1.5rem;
        border: 2px solid transparent;
        border-radius: 5px;
        padding: 10px;
        transition: all 0.3s ease;
    }

    .link:hover {
        border: 2px solid;
    }
    .container {
        display: flex;
        width: 100%;
        height: auto;
        flex-direction: column;
        justify-content: center;
        align-items: center;
    }
    .locker-card {
        background-color: rgba(0, 0, 128, 0.3);
        padding: 2vw 5vw;
        display: flex;
        gap: 5rem;
        flex-direction: row;
        border-radius: 30px;
    }
    form {
        text-align: right;
    }

    input {
        color: #fff;
        /* margin-top: 1vw;  */
        height: 3vw;
        background-color: transparent;
        border-bottom: solid 1px #fff !important;
        border: none;
        outline: none;
        /* display: flex; */
        width: 20vw;
        margin-right: 2vw;
    }
    button {
        font-family: 'Outfit';
        padding: 0 1vw;
        font-size: 1rem;
        background-color: transparent;
        color: #fff;
        border: 2px solid;
        border-radius: 5px;
        border-color: #ffda93;
        color: #fff;
        outline: none;
        cursor: pointer;
        transition: all 0.25s ease-in-out;
    }

    button:hover {
        color: #ffda93;
    }
    a {
        text-decoration: none;
        color: #fff;
    }
</style>
<body>
    <nav>
        <label class="logo"><a href="/">SoleSeva</a></label>
        <ul class="links">
            <li><a href="/" class="link">Home</a></li>
            <li><a href="/about" class="link">About</a></li>
            <li><a href="/dashboard" class="link">Dashboard</a></li>
            <li><a href="/locker" class="link">Lockers</a></li>
        </ul>
        <ul class="loginUl">
            <li><a href="/auth/logout" class="link">Logout</a></li>
        </ul>

    </nav>

    <div class="container">
        <h1 style="color: #ffda93; font-size: 3rem;">Your Lockers</h1>
            <% lockers.forEach(locker => {%>
                <% var date1 = new Date() %>
                <% var date2 = new Date(parseInt(locker.regTime)) %>
        
                    <div class="locker-card">
                        <div class="creds">
                            <h1>Locker No. <%= locker.number %></h1>
                            <h2>Size: <%= locker.size %></h2>
                            <p>Time Elapsed: <%= Math.round(Math.abs(date1 - date2)*60 / 36e5) %> minutes</p>
                        </div>
                        <form action="/locker/retrieve/<%=locker.number%>" method="post">
                            <h3>Enter Password sent to Registered Mobile Number</h3>
                            <input type="text" name="otp" id="otp" placeholder="Enter password here">
                            <button type="submit"><h2>Unlock</h2></button>
                        </form>
                    </div>
                   <br>
            <% }); %>
    </div>

</body>
</html>